#!/bin/bash

echo "Updating configs for production..."

# This script will be run manually for each service
# Just follow the pattern for each service's application.yml

echo "Done! Now update each service manually following the patterns shown earlier"
